<?php
	error_reporting(E_ALL);
    ini_set('display_errors', true);// Функции с плащами пока вырезаны
    require_once $_SERVER['DOCUMENT_ROOT'] . "/define.php";

require __CM__ . "inc/mysql.php";

// if (isset(__URL__[2]) && __URL__[2] === 'gravitlauncher') {
//     // Get the URI parameters
//     $uriParameters = http_build_query($_GET);

    // Use absolute path for inclusion with URI parameters
// $moduleFilePath = __CD__ . "modules/api/skins/skinview.php";

    // Check if the file exists before including
    // if (file_exists($moduleFilePath)) {
// include $moduleFilePath;
//     } else {
//         // Handle the case where the file doesn't exist
//         die("Module file not found.");
//     }
// }
  